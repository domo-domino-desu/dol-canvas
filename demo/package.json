{
  "name": "@dol-canvas/demo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "rsbuild dev --config rsbuild.config.ts",
    "dev:local": "rsbuild dev --config rsbuild.config.ts",
    "dev:cdn": "DOL_CANVAS_SOURCE=cdn rsbuild dev --config rsbuild.config.ts",
    "build": "rsbuild build --config rsbuild.config.ts",
    "build:local": "rsbuild build --config rsbuild.config.ts",
    "build:cdn": "DOL_CANVAS_SOURCE=cdn rsbuild build --config rsbuild.config.ts",
    "preview": "rsbuild preview --config rsbuild.config.ts"
  },
  "dependencies": {
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@rsbuild/core": "^2.0.6",
    "@rsbuild/plugin-vue": "^1.2.8",
    "typescript": "^5.9.0"
  }
}
